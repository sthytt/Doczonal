<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Doczonal.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Doczonal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Doczonal">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="OpenFile" Label="Load   " Click="LoadImage_Click"/>
            <AppBarButton Icon="Add" Label="Draw" Click="DrawMode_Click"/>
            <AppBarButton Icon="Edit" Label="Drag" Click="SelectMode_Click"/>
            <AppBarButton Icon="Clear" Label="Clear All" Click="ClearZones_Click"/>
            <AppBarButton Icon="Go" Label="Done" Click="Done_Click" Margin="40,0,0,0"/>
        </CommandBar>

        <Border Grid.Row="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" ZoomMode="Enabled" MinZoomFactor="0.1" MaxZoomFactor="5.0">
                <Grid x:Name="ImageContainer" Width="800" Height="600" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image x:Name="BackgroundImage" Stretch="Uniform" />
                    <Canvas x:Name="DrawingCanvas" Background="Transparent" 
                            PointerPressed="Canvas_PointerPressed" 
                            PointerMoved="Canvas_PointerMoved" 
                            PointerReleased="Canvas_PointerReleased" />
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
